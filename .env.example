# Database connection string (for PostgreSQL)
# For local PostgreSQL (recommended for Windows users):
#     postgresql://username:password@localhost:5432/mydatabase
# For Neon cloud database (works on all platforms):
#     postgres://username:password@endpoint.neon.tech/neondb?sslmode=require
DATABASE_URL=

# Session secret (generate a random string)
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_session_secret_here

# Port for the server to listen on (default is 5000)
# If you're getting an EADDRINUSE error, change this to another port
PORT=5000

# Network address binding
# If you're on Windows and experiencing connection issues, try:
# HOST=localhost
#
# If you're getting errors when connecting to the database on Windows, 
# you might need to create a separate PostgreSQL user with a password.
# See README.md for more Windows-specific setup instructions.
HOST=0.0.0.0