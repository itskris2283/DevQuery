# Database connection string (for MySQL)
# For local MySQL (recommended for Windows users):
#     mysql://username:password@localhost:3306/mydatabase
# For PlanetScale cloud database (works on all platforms):
#     mysql://username:password@aws.connect.psdb.cloud/mydatabase?sslmode=required
DATABASE_URL=

# MySQL configuration (used if DATABASE_URL is not a complete connection string)
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=mydatabase
MYSQL_HOST=localhost

# Session secret (generate a random string)
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_session_secret_here

# Port for the server to listen on (default is 5000)
# If you're getting an EADDRINUSE error, change this to another port
PORT=5000

# Network address binding
# If you're on Windows and experiencing connection issues, try:
# HOST=localhost
#
# If you're getting errors when connecting to the database on Windows, 
# you might need to create a separate MySQL user with a password.
# See README.md for more Windows-specific setup instructions.
HOST=0.0.0.0